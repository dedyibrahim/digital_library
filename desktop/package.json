{
  "name": "pustaka-desktop",
  "version": "1.0.1",
  "description": "Digital Library desktop folder synchronization",
  "author": "Digital Library",
  "private": true,
  "main": "src/main.cjs",
  "scripts": {
    "prestart": "node scripts/make-icon.cjs",
    "predist": "node scripts/make-icon.cjs",
    "prepack": "node scripts/make-icon.cjs",
    "start": "electron .",
    "test": "node --test test/*.test.cjs",
    "smoke": "electron scripts/smoke.cjs",
    "smoke:transfer": "electron scripts/transfer-smoke.cjs",
    "dist": "electron-builder --win nsis --x64",
    "pack": "electron-builder --dir"
  },
  "devDependencies": {
    "@resvg/resvg-js": "^2.6.2",
    "electron": "44.3.0",
    "electron-builder": "26.15.3"
  },
  "build": {
    "appId": "id.pustaka.desktop",
    "productName": "Digital Library",
    "files": [
      "src/**/*",
      "package.json"
    ],
    "directories": {
      "output": "dist"
    },
    "win": {
      "target": "nsis",
      "icon": "src/icon.ico",
      "signAndEditExecutable": true,
      "signExecutable": false
    },
    "nsis": {
      "oneClick": false,
      "perMachine": false,
      "allowToChangeInstallationDirectory": true,
      "createDesktopShortcut": true,
      "installerIcon": "src/icon.ico",
      "uninstallerIcon": "src/icon.ico",
      "artifactName": "Digital-Library-Setup-${version}.${ext}"
    }
  }
}
